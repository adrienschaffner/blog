<h1>Welcome !</h1>

<h3>We have <%= Article.public_count %> articles available !</h3>
<p>In here you will find every article I wrote. <br>
 I post time to time, my goal is to give an overview of the Product Manager role and any aspect I find interesting.</p>

<%# <ul> %>
  <% @articles.each do |article| %>
    <div class="row">

      <% unless article.archived? %>
        <%# <li> %>
        <div class="col-xs-8 col-xs-offset-2">
          <div class="well well-lg">
            <div class="article-title">
        <%# <a href="/articles/<%= article.id %>
              <%=link_to article.title.capitalize, article %>
            </div>
      <%# </li> %>
      <%# <li> %>
            <div class="article-body">
              <%= truncate(article.body, length: 100) %>
            </div>
        <%# <%= article.rating %>
        <%# <%= article.author %>
        <%# <%= article.body.slice(0...25) %>
      <%# </li> %>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
  <% if admin_signed_in?  %>
    <%= link_to "New Article", new_article_path %>
  <% else %>
    <%# <%= link_to "sign in",  admin_session_path %>
    <%= link_to "sign in",  new_admin_session_path %>
  <% end %>
<%# </ul> %>

<%# <%= link_to "top articles", top_articles_path %>

      <%# <div class="article-actions">
          <%= link_to "New Article", new_article_path, class: "btn btn-xs btn-primary" %>
          <%# <%= link_to "Delete", article_path(article), method: :delete, data: { confirm: "Are you sure you want to delete this article?"}, class: "btn btn-xs btn-danger" %>
      <%# </div>  %>
